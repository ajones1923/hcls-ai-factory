# Drug Discovery Pipeline Environment Configuration
# Copy this file to .env and fill in the required values

# =============================================================================
# REQUIRED: NVIDIA API Keys
# =============================================================================
# NGC API Key (for local NIM containers): https://ngc.nvidia.com/setup/api-key
NGC_API_KEY=your-ngc-api-key-here

# NVIDIA API Key (for Cloud NIM endpoints): https://build.nvidia.com/
# Required when NIM_MODE=cloud
NVIDIA_API_KEY=your-nvidia-api-key-here

# =============================================================================
# NIM Mode: "cloud" or "local"
# =============================================================================
# cloud = NVIDIA hosted APIs (no local GPU required, works on ARM64/DGX Spark)
# local = Docker containers (requires x86_64 + NVIDIA GPU)
# If not set, auto-detects based on API key format (nvapi- prefix = cloud)
NIM_MODE=cloud

# =============================================================================
# Service URLs
# =============================================================================

# Drug Discovery UI port (default: 8505)
DD_UI_PORT=8505

# Cloud NIM endpoint URLs (used when NIM_MODE=cloud)
MOLMIM_URL=https://health.api.nvidia.com/v1/biology/nvidia/molmim/generate
DIFFDOCK_URL=https://health.api.nvidia.com/v1/biology/mit/diffdock

# Local NIM container URLs (used when NIM_MODE=local)
# MOLMIM_URL=http://localhost:8001
# DIFFDOCK_URL=http://localhost:8002

# =============================================================================
# OPTIONAL: Development/Demo Settings
# =============================================================================

# Allow mock NIM fallback (for development/testing only!)
# Set to "true" to use mock data when NIM services are unavailable
# WARNING: Mock data is NOT scientifically valid
NIM_ALLOW_MOCK_FALLBACK=false

# =============================================================================
# OPTIONAL: Monitoring (Grafana)
# =============================================================================

# Grafana admin credentials
GRAFANA_USER=admin
GRAFANA_PASSWORD=your-secure-password

# =============================================================================
# GTC Demo Quick Start:
# =============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Set NGC_API_KEY with your key
# 3. Set GRAFANA_PASSWORD to something secure
# 4. Run: docker-compose up -d
