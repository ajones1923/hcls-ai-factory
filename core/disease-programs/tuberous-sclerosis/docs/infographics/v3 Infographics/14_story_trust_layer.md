# Slide 14 — Why you can trust it

## 🎙️ Narration script (~60 sec)

In medicine, a confident-sounding AI that you can't check is worse than no AI at all. So trust was a
*design* requirement here, not a feature we bolted on. Five disciplines run through the whole engine.
[beat]

One — provenance. Every single output carries a paper trail back to its source: which record, which
guideline, which calculation. Nothing is "because the AI said so."

Two — the right tool for each job. The forecasting is classical statistics, not a chatbot. The
classification follows a deterministic rulebook. Language models are used only where judgment over
text actually helps — and even then they cite their sources.

Three — honest uncertainty. Every forecast carries its prediction interval, like that hurricane cone.
Four — disciplined alerts: no more than three a week per clinician, so the signal never drowns in
noise. And five — it degrades safely: if a model is unavailable, it falls back to a local one and
tells you, rather than guessing.

Provenance, the right tool, honest uncertainty, restraint, safe failure. [beat] That's what makes it
something a clinician can actually trust.

---

## 🖼️ Nano Banana Pro prompt

HOUSE STYLE — keep identical across all 18 frames (this is frame 14 of 18). 16:9 landscape. Background deep navy #0F1830; panels #16213F with thin #26345C borders; primary accent sky-blue #7FB0FF; key numbers & highlights in gold #FFD166; body text off-white #E8EEF9. Clean modern geometric sans-serif, strong hierarchy, generous spacing, premium scientific-keynote feel — credible, never a marketing cartoon. Small pink "SYNTHETIC · not FDA-cleared" pill top-right; a thin footer band; a small gold "14 / 18" index in the bottom-right corner. Render all text crisply and spelled exactly as written. (Everything in this HOUSE STYLE note is design direction — do NOT print any of its words on the image, including "premium scientific-keynote", "frame", "house style", or "index"; it only contributes the small gold index number in the bottom-right corner. Structural words below such as "header", "footer", "strip", "band", "panel", "column", "spine" are layout directions, not text to draw — render on the image as text only the words shown inside quotation marks.)

Create FRAME 14 — THE TRUST LAYER (five disciplines): a calm, confident grid of exactly five pillars/cards across the frame, each with a simple line icon, a short title, and one line — conveying that trust is engineered in. A unifying gold line beneath ties them together.

Across the top, a header — "Built to Be Trusted", subtitle "Trust as a design requirement, not a feature".
FIVE CARDS (each once, no duplicates):
1. "Provenance on every output" (paper-trail / receipt icon) — "Every claim links back to its source record, guideline, or computation. Nothing is 'because the AI said so.'"
2. "The right tool for each job" (wrench + branching icon) — "Classical statistics for forecasts, a deterministic rulebook for classification, LLMs only where text-judgment helps — and they cite sources."
3. "Honest uncertainty" (forecast-cone icon) — "Every prediction ships with its 50% / 90% interval — like a hurricane cone. Confidence is shown, never implied."
4. "Disciplined alerts" (quiet-bell icon) — "A hard cap of ≤ 3 alerts per clinician per week. Signal, never noise — so the important ones land."
5. "Fails safe" (shield / fallback icon) — "If a model is unavailable it falls back to a local one and says so. It degrades transparently rather than guessing."
BOTTOM GOLD TIE-LINE: "Provenance · right tool · honest uncertainty · restraint · safe failure — the foundation a clinician can actually rely on."
Along the very bottom, a thin footer strip reading: "TSC Intelligence Engine · Engine 7 of the HCLS AI Factory · Apache 2.0 · event-sourced & fully replayable · SYNTHETIC demonstration data · decision support, clinician review required · not FDA-cleared."

A calm grid of exactly five pillars, each with one crisp line, the gold tie-line beneath. Confident and trustworthy. Every label sharp.
