# Slide 07 — How the AI is used responsibly

## 🎙️ Narration script (~60 sec)

People hear "AI engine" and assume a chatbot is making the calls. It isn't — and that's deliberate.
[beat]

The rule we built around is: be deterministic where correctness matters, and use AI only at the
edges. So the things that have to be *right* — the genetic verdict, the coordination logic, the
forecasting math — those are fixed, auditable code. They produce the same answer every time, and an
AI never overrules them.

Where we *do* use large language models, it's only for what they're genuinely good at: reading messy
text and writing clear explanations. And we route each step to the right-sized model by its
stakes — a fast one for light extraction, a balanced one for analysis, the most capable one for the
high-stakes genetic narrative — with a local model as an offline fallback, and a ledger tracking
every token and dollar.

The principle, in one line: an AI *explains*; it never *decides* the genetic verdict. The rulebook
does. [beat] That's what makes this safe to put near a clinical decision.

---

## 🖼️ Nano Banana Pro prompt

**HOUSE STYLE — keep identical across all 18 frames (this is frame 07 of 18).** 16:9 landscape. Background deep navy #0F1830; panels #16213F with thin #26345C borders; primary accent sky-blue #7FB0FF; key numbers & highlights in gold #FFD166; body text off-white #E8EEF9. Clean modern geometric sans-serif, strong hierarchy, generous spacing, premium scientific-keynote feel — credible technical diagram, never a marketing cartoon. Small pink "SYNTHETIC · not FDA-cleared" pill top-right; a thin footer band; a small gold **"07 / 18"** index in the bottom-right corner. Render all text crisply and spelled exactly as written; **exactly four LLM-tier cards, no extra or duplicated cards.**

Create **FRAME 07 — AI UNDER THE HOOD (used responsibly)**: a technical diagram — a "stakes router" splitting into a **teal-green #2FA37A 'deterministic core' lane** (top) and a **sky-blue 'LLM tiers' lane** (bottom, models increasing in capability left to right), with the cost ledger at right and the governing principle prominent at the bottom.

**TOP HEADER:** "AI Under the Hood — Used Responsibly", subtitle "Determinism where correctness matters · LLMs only at the edges · stakes-routed".
**LANE 1 (teal-green, top) "DETERMINISTIC CORE — not an LLM":** three cards — "ACMG-AMP variant classifier (the authoritative verdict)", "Event-sourced orchestrator (coordination)", "Classical forecasting (mixed-effects + Gaussian process)". A gold note: "Fixed, auditable, reproducible. An LLM never overrules them."
**LANE 2 (sky-blue, bottom) "LLM EDGES — text in, narrative out":** EXACTLY FOUR cards, left to right, increasing capability: "Haiku — fast, light extraction" → "Sonnet — analysis" → "Opus — high-stakes narrative (ACMG explanation)" → "Local model — offline fallback". Label: "Each step routed to the right-cost model by its stakes."
**RIGHT — "COST LEDGER" panel:** "per-tier tokens + USD tracked, with a spend cap — economics observable and bounded."
**BOTTOM CALLOUT BAND (gold-bordered):** "The principle: an AI explains; it never decides the genetic verdict. The rulebook does."
**FOOTER:** "Tiered models + local fallback · cost ledger · Apache 2.0 · Engine 7 of the HCLS AI Factory · SYNTHETIC demonstration data · not FDA-cleared."

A clear router splitting into a green deterministic lane (top) and a blue LLM-tier lane (bottom), cost ledger at right, principle prominent at bottom. Every label sharp.
