# Nano Banana Pro prompt — ARCHITECTURE: AI under the hood (model tiering)

How the engine uses AI *responsibly*: deterministic core where correctness matters, LLMs only
at the edges, stakes-routed across model tiers, with a cost ledger. Copy the block below into
Nano Banana Pro.

> **Frame 07 / 18 · 16:9.** The "AI under the hood" frame of the TSC AI Engine storyboard (see `STYLE_PREAMBLE.md`).

---

## Prompt

**HOUSE STYLE — keep identical across all 18 frames (this is frame 07 of 18).** 16:9 landscape. Background deep navy #0F1830; panels #16213F with thin #26345C borders; primary accent sky-blue #7FB0FF; key numbers & highlights in gold #FFD166; body text off-white #E8EEF9. Clean modern geometric sans-serif, strong hierarchy, generous spacing, premium NVIDIA / scientific-keynote feel — credible technical diagram, never a marketing cartoon. Small pink "SYNTHETIC · not FDA-cleared" pill top-right; a thin footer band; a small gold **"07 / 18"** index in the bottom-right corner. Render all text crisply and spelled exactly as written.

Create **FRAME 07 — AI UNDER THE HOOD (used responsibly)**: a technical diagram of how the engine routes work — a "stakes router" splitting into a **teal-green #2FA37A 'deterministic core' lane** (top) and a **sky-blue 'LLM tiers' lane** (bottom, models increasing in capability left to right), with the cost ledger at right and the governing principle prominent at the bottom.

**TOP HEADER (full width):** Title **"AI Under the Hood — Used Responsibly"**, subtitle **"Determinism where correctness matters · LLMs only at the edges · stakes-routed"**; small pink pill top-right **"SYNTHETIC · not FDA-cleared"**.

**CENTER — a "STAKES ROUTER" node splitting work into two lanes:**

**LANE 1 (teal-green, top), titled "DETERMINISTIC CORE — not an LLM":** three cards — *"ACMG-AMP variant classifier (the authoritative verdict)"*, *"Event-sourced orchestrator (coordination)"*, *"Classical forecasting (mixed-effects + Gaussian process)"*. A gold note: *"These are fixed, auditable, reproducible. An LLM never overrules them."*

**LANE 2 (blue, bottom), titled "LLM EDGES — text in, narrative out":** four model-tier cards left to right, increasing in capability: **"Haiku — fast, light extraction"** → **"Sonnet — analysis"** → **"Opus — high-stakes narrative (ACMG explanation, verification)"** → **"Local Llama — offline fallback"**. A label: *"Each step routed to the right-cost model by its stakes."*

**RIGHT SIDE — a small "COST LEDGER" panel:** *"per-tier tokens + USD tracked, with a spend cap — economics observable and bounded."*

**BOTTOM CALLOUT BAND (gold-bordered):** **"The principle: an AI explains; it never decides the genetic verdict. The rulebook does."**

**FOOTER (thin):** *"Tiered Claude models + local fallback · cost ledger at /system/usage · Apache 2.0 · Engine 7 of the HCLS AI Factory · SYNTHETIC demonstration data · not FDA-cleared."*

Composition: a clear router splitting into a green "deterministic" lane (top) and a blue "LLM tiers" lane (bottom, models increasing in capability left to right), the cost ledger on the right, the principle prominent at the bottom. Credible technical-systems look, not a cartoon. Every label sharp and readable.

---

## Tips

- The teaching point is **two lanes: deterministic (green) vs. LLM (blue)** — if it blurs, add:
  *"clearly separate the green deterministic lane from the blue LLM-tier lane."*
- Truthful (Haiku/Sonnet/Opus + local Llama, deterministic ACMG/orchestrator/forecasting, cost
  ledger). Keep the SYNTHETIC / not-FDA-cleared labels.
- Text second pass: *"keep the exact two-lane layout but regenerate only the text so every label
  is sharp and correctly spelled."*
