# ═══════════════════════════════════════════════════════════════════════════
# HCLS AI Factory — .gitignore  (single source of truth → github.com/ajones1923/hcls-ai-factory)
# The 711 G working tree lives on the DGX Spark; only code + docs are published.
# ═══════════════════════════════════════════════════════════════════════════

# ── BIG DATA (the 534 G genomics + all data dirs) — never publish ─────────────
data/
hcls-ai-factory-core-data/
*.bam
*.bai
*.cram
*.crai
*.sam
*.fastq
*.fastq.gz
*.fq
*.fq.gz
# reference-genome files & indices
*.fa.bwt
*.fa.pac
*.fa.sa
*.fa.amb
*.fa.ann
*.fa.fai
*.dict
GRCh3?*
*.2bit

# ── MODEL WEIGHTS (recreate/download) ────────────────────────────────────────
*.pt
*.pth
*.safetensors
*.ckpt
*.onnx
*.h5
*.pb
*.bin

# ── VENDORED EXTERNAL REPOS (recreate via git clone; also carry their own .git)─
vendor_*/
**/vendor_*/

# ── PYTHON ENVIRONMENTS & CACHES ─────────────────────────────────────────────
venv/
.venv/
env/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/
node_modules/
build/
dist/

# ── SEPARATE REPOS (their own git; not part of this monorepo) ────────────────
landing-page/

# ── LOGS & RUNTIME ───────────────────────────────────────────────────────────
*.log
logs/
*.pid

# ── ARCHIVES / large media ───────────────────────────────────────────────────
*.tar
*.tar.gz
*.tgz
*.zip
*.mp4
*.mov
*.webm

# ── SECRETS & LOCAL CONFIG ───────────────────────────────────────────────────
.env
.env.*
!.env.example
*.key
*.pem
secrets/

# ── Vendor / edition-specific builds — never publish (keep OSS repo neutral) ──
other-versions/
aios/
*VAST*
*vast*
*AI_OS*
*ai_os*
*AI-OS*
*ai-os*

# ── LOCAL TOOLING (per-machine; not part of the repo) ────────────────────────
.claude/

# ── OS / EDITOR ──────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp

# ── Config env files (publish only .env.example templates) ───────────────────
*.env

# ── Image / design binaries (keep docs as markdown; use Git LFS/Releases) ────
*.png
*.jpg
*.jpeg
*.gif
*.bmp
*.tiff
*.webp
*.psd
*.ai
*.emmx
*.mmx
media/infographics-updates/

# keep the tiny demo fixture (small VCF + readme for reproducibility)
!data/demo/
!data/demo/**

# ── Agent sub-repos (their own GitHub repos; not part of this monorepo) ───────
core/engines/clinical-imaging/agent
core/engines/precision-oncology/agent

# scratch notes — not for publish
tmp/
