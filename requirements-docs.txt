# Public docs site generator — pinned for a reproducible MkDocs build (Pillar 16).
# Netlify builds with: pip install -r requirements-docs.txt && mkdocs build --strict
mkdocs-material[imaging]==9.5.44
mkdocs-gen-files==0.5.0
